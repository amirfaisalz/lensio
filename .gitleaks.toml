# Lensio Gitleaks Configuration
# Enforces zero secrets committed while allowlisting deterministic synthetic fixtures and test mocks.

title = "Lensio Gitleaks Secret Detection Policy"

[extend]
useDefault = true

[allowlist]
description = "Global allowlist for synthetic test data, fixtures, documentation, and mock keys"
paths = [
  '''tests/fixtures/synthetic/.*''',
  '''tests/fixtures/.*''',
  '''tests/.*_test\.go''',
  '''apps/dashboard/src/.*\.test\.(ts|tsx)''',
  '''openapi/openapi\.yaml''',
  '''\.env\.example''',
  '''docker-compose\.yml''',
  '''DEVELOPMENT_TRACKING\.md''',
  '''PRD\.md''',
  '''AGENTS\.md'''
]
regexes = [
  '''lensio_(live|test)_[a-zA-Z0-9]{32,}''',
  '''lensio_dev_password''',
  '''lensio_test_password''',
  '''req_[0-9A-Z]{10,}''',
  '''AIzaSy[A-Za-z0-9_-]{33}'''
]
