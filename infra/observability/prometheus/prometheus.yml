global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'nusaid-api'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['api:8080', 'host.docker.internal:8080']
        labels:
          service: 'nusaid-api'
          env: 'development'
